version: '3.8'

services:
  api:
    volumes:
      - ./backend:/app
    environment:
      - APP_ENV=development

  frontend:
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=development

  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"